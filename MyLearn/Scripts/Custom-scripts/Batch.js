function Batch(Name, Value, BatchPhoto, BatchDescription,State) {
    this.Name = Name;
    this.Value = Value;
    this.BatchPhoto = BatchPhoto;
    this.BatchDescription = BatchDescription;
    this.State = State;
}